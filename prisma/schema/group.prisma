model Group{
    id String @id @default(uuid())
    name String
    description String?
    allowMembersToInvite Boolean @default(true)
    allowMembersToManageCategory Boolean @default(false)

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    members GroupMember[]
    category Category[]
}